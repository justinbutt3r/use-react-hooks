import { renderHook } from "@testing-library/react-hooks";
import {{name}} from "./{{name}}";

const setup = () => renderHook(() => {{name}}());

it("should be null", () => {
  const { result } = renderHook(() => {{name}}());

  expect(result.current).toBeNull();
});
